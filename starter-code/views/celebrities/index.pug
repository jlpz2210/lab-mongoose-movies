extends ../layout.pug

block content
  title Lista de Celebridades
  br
  body
    each celeb in celebrities
      div
        p=celeb.name
        div
          a(href=`/celebrities/${celeb._id}`) Detalle
          form(action=`/celebrities/${celeb._id}/delete` method="POST")
            button(type="submit") Eliminar