div
  form(action=`/movies/${movie._id}` method="POST")
    div
      label Title:
        input(type="text" name="title" value=movie.title)
    div
      label Genre:
        input(type="text" name="genre" value=movie.genre)
    div
      label Plot:
        textarea(name="plot", cols="30", rows="10")=movie.plot
    button(type="submit") Guardar